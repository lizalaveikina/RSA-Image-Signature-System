# RSA-Image-Signature-System
Digitally sign and verify PNG images using RSA (4096-bit) and SHA-256. The signature is embedded in the image metadata without altering its visual content. Built with Python, Pillow, and cryptography.
